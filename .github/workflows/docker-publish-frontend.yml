name: Build and Push Docker Image

on:
  push:
    branches:
      - develop
#    paths:
#      - frontend/**

jobs:
  build:
    uses: ./.github/workflows/build.yml
    with:
      image_name: frontend
      tag: latest-develop